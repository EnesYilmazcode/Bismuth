VITE_SUPABASE_URL="http://127.0.0.1:8765"

# Optional. Required only for the Benchmark mode, which proxies prompts to
# multiple AI models via OpenRouter (https://openrouter.ai). Without this,
# the /benchmark route shows a setup screen instead of running comparisons.
# Get a key from https://openrouter.ai/keys
OPENROUTER_API_KEY=""
